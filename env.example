DEBUG=True
CORS_ALLOWED_ORIGINS=http://localhost:3000,
DEVELOPMENT_SERVER=True
SECRET_KEY=q$%5ryvbn!g5y96jwp0(#zfuc)lia*mi32i4qt2mz0jdt7yrx_h

# Database Configuration (Production)
# Use PostgreSQL for production. Leave empty to use SQLite (development only)
DATABASE_ENGINE=django.db.backends.postgresql
DATABASE_NAME=tamaade_db
DATABASE_USER=postgres
DATABASE_PASSWORD=your_secure_password
DATABASE_HOST=localhost
DATABASE_PORT=5432

# ImageKit Configuration (Recommended for production CDN delivery)
# Sign up at https://imagekit.io/
# Get credentials from ImageKit dashboard
IMAGEKIT_PRIVATE_KEY=
IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_public_key/

# AWS S3 Configuration (Alternative to ImageKit)
# Leave empty if using ImageKit instead
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_STORAGE_REGION=
AWS_S3_CUSTOM_DOMAIN=
AWS_DEFAULT_ACL=public-read
AWS_AUTO_CREATE_BUCKET=True

# Hubtel Mobile Money Integration
# Get credentials from Hubtel dashboard at https://hubtel.com
HUBTEL_BASIC_AUTH_KEY=
HUBTEL_POS_SALES_ID=
HUBTEL_CALLBACK_URL=https://your.domain.com/payments/api/callback/
# Optional: comma-separated list of Hubtel source IPs for additional verification
HUBTEL_ALLOWED_IPS=52.58.0.0/16,52.59.0.0/16

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_PORT=587
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=contact@tamaade.com

# Redis Configuration (For Celery task queue)
REDIS_URL=redis://redis:6379/1

# Additional Settings
SECURE_SSL_REDIRECT=False
ACCOUNT_EMAIL_VERIFICATION=optional
CELERY_TASK_ALWAYS_EAGER=False
